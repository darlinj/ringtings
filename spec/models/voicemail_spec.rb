require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe Voicemail do
  #before do
    #username = "Bob"
    #password = "secret"
    #@options = {:basic_auth => {:username => username, :password => password}}

    #from = "0123456789"
    #date_created = 1.hour.ago
    #priority = "normal"
    #last_heard = 10.minutes.ago
    #duration = 10.seconds
    #filename = "gibberish.wav"


    #Voicemail.stub(:get).with(VOICEMAIL_URI, @options)
    #@voicemails = [vm1,vm2]
  #end
  #describe "#find_all" do
    #it "should set the authentication credentials" do
      #Voicemail.should_receive(:get).with(VOICEMAIL_URI, @options)
      #Voicemail.new(username,password).get
    #end

    #it "should return the list of voicemails" do

      #Voicemail.new(username,password).get.should == @voicemails
    #end

  #end
end 
